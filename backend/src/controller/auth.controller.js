const signup = (req, res) => {
  console("Signup");
};

const login = (req, res) => {
  console("login");
};
const logout = (req, res) => {
  console("logout");
};

module.exports = { signup, login, logout };
